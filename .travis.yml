language: go
go:
- '1.10'
sudo: false
install:
- go get -v github.com/alecthomas/gometalinter
- gometalinter --install
script:
  - export PATH=$PATH:$HOME/gopath/bin
  - go get  ./...
  - ./goclean.sh
  - go test -v ./...
notifications:
  email:
    on_success: never
    on_failure: always
